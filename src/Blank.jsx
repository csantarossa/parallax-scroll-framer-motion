import React from "react";

const Blank = ({ prop }) => {
  return (
    <div className="h-screen w-full flex justify-center items-center font-bold text-9xl">
      {prop}
    </div>
  );
};

export default Blank;
